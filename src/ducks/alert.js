const ALERT = 'ALERT';

const reducer = (state = {}, action) => {
  switch (action.type) {
    default:
      return state;
  }
};


export const alert = () => ({ type: ALERT });
export default reducer;
